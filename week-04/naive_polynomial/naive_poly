def naive_polynomial(poly,x):
    p = poly[0]
    for i in range(1,len(poly)):
        power = 1
        for j in range(1,i):
            power = power * x
        p = p + poly[i]*power
    return p

points,x = ([1,2,3],2)
res = naive_polynomial(points,x)
print(res)